# macOS
.DS_Store

# Node
/node_modules/
/package-lock.json

# Built Etsy artifacts
etsy-package/output/
etsy-package/FaceQuest-v*/

# ZIP excluded: exceeds GitHub 100MB limit (face-api.js models ~209MB).
# Rebuild locally with `node etsy-package/build-package.js`.
etsy-package/FaceQuest-v*.zip
